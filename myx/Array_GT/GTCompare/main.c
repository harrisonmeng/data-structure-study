//
//把下面两个广义表用单链表存储，然后用递归方式遍历并且比较两个表，打印出不同的地方
//A( 'a', 'b', 'c', (1, 2, 3, (x, y, z), 4), 'd')
//B( 'a', 'b', 'c', (1, 2, 3, (x, y, u), 4), 'd')




#include <stdio.h>
#include <stdlib.h>



int main()
{

    return 0;
}
